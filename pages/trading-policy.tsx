import { NextPage } from "next";
import React, { useMemo } from "react";
import { Container } from "react-bootstrap";

const TradingPolicy: NextPage = () => {
  return useMemo(() => <Container>Đang phát triển</Container>, []);
};

export default TradingPolicy;
