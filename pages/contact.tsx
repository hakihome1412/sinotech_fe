import { NextPage } from "next";
import React, { useMemo } from "react";
import { Container } from "react-bootstrap";

const Contact: NextPage = () => {
  return useMemo(() => <Container>Đang phát triển ....</Container>, []);
};

export default Contact;
